testing = [1, 2, 3, 4, 5] # defines an array

for i in range(len(testing)):
    print("chapter " + str(testing[i])) # adds string 'chapter' and concatenates it with string of all entries in testing based on i indexing.